lab4:
	javac *.java
clean:
	rm *.class
	rm Emulator.ReferenceTables/*.class
	rm Emulator.Instructions/*.class
	rm Emulator.Instructions/RegisterInstructions/*.class
	rm Emulator.Passes/*.class
	rm Emulator.Modules/*.class